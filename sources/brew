#!/usr/bin/env bash

if [ `which brew 2>/dev/null | wc -l` -lt 1 ]; then
  return
fi

export PATH=/usr/local/bin:${PATH}

brew=/usr/local/bin/brew

function brewup() {
  $brew update
  $brew upgrade
  $brew cleanup
}
